version: '3.8'
services:
  sandy-pro:
    build: .
    ports:
      - "5000:5000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./uploads:/app/uploads
      - ./ml_models:/app/ml_models
    restart: unless-stopped
